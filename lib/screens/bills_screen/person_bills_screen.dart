import 'package:flutter/material.dart';

class PersonBillsScreen extends StatelessWidget {
  const PersonBillsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
